# * this method should use python -m to make sure the hydra config is loaded correctly
# hydra config
hydra:
  run:
    dir: ${log_path}
  job:
    chdir: false

# Path
paths:
  sam3d_body_results_path: /workspace/data/sam3d_body_results/person
  video_path: /workspace/data/side_raw # this is raw data path, with left and right side view.
  inference_output_path: /workspace/data/vggt_npy/${infer.view}

log_path: logs/vggt/${infer.view}/${now:%Y-%m-%d}/${now:%H-%M-%S}

# Model
model:
  ckpt_path: /workspace/code/ckpt/pretrained_h36m_detectron_coco.bin

visualize:
  save_video: true
  plot_3d: true
  plot_2d: true
  plot_skeleton: true
  plot_skeleton_3d: true

infer:
  conf_thres: 50.0
  prediction_mode: "Depthmap and Camera Branch"
  keep_frames: true
  gpu: 1
  reproj_err: 20
  hflip: true
  view: "single" # "single" or "dual"
  frame_interval: 1

runtime:
  verbose: true
  num_workers: 4
  debug: false

dataset:
  recursive: false
